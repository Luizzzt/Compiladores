Compilador Fase1 - Análise Léxica e Sintática (Portugol)

Como compilar:
    gcc -Wall -Wno-unused-result -g -Og compilador.c -o compilador

Como executar:
    ./compilador <arquivo_fonte>   git add -A
   git commit --amend -m "Estrutura inicial do projeto: main e README"
   git push --force